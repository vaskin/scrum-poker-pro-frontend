import { css } from '@chakra-ui/react';

export const round = css({
  width: '12px',
  height: '12px',
  border: '2px solid #F5F7F9',
  borderRadius: '50%',
});

export const roundWrapper = css({
  height: '100%',
  justifyContent: 'center',
  alignItems: 'center',
});
